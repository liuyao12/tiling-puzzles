# One Short

A simple, no-hints L-triomino tiling puzzle. Choose a board size, rotate the
piece, and cover every square except the missing one.

Open `index.html` directly or serve the folder with any static web server.
