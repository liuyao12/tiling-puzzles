# One Short

A simple, no-hints L-triomino tiling puzzle. Choose rectangular board
dimensions, rotate the pieces, and fill the playable region.

Open `index.html` directly or serve the folder with any static web server.
